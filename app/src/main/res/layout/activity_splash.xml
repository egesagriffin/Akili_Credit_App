<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/green_dark"
    android:padding="32dp">

    <!-- Logo circle -->
    <FrameLayout
        android:layout_width="120dp"
        android:layout_height="120dp">

        <androidx.cardview.widget.CardView
            android:layout_width="120dp"
            android:layout_height="120dp"
            app:cardCornerRadius="60dp"
            app:cardBackgroundColor="@color/white"
            app:cardElevation="8dp"
            xmlns:app="http://schemas.android.com/apk/res-auto">

            <ImageView
                android:id="@+id/iv_logo"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_menu_compass"
                android:tint="@color/green_mid" />

        </androidx.cardview.widget.CardView>
    </FrameLayout>

    <!-- App name -->
    <TextView
        android:id="@+id/tv_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:text="Akili Credit"
        android:textColor="@color/white"
        android:textSize="28sp"
        android:textStyle="bold"
        android:letterSpacing="0.03"
        android:alpha="0" />

    <!-- Tagline -->
    <TextView
        android:id="@+id/tv_tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Alternative Credit Scoring System"
        android:textColor="#CCFFFFFF"
        android:textSize="14sp"
        android:alpha="0" />

    <!-- Sub tagline -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Powered by Mobile Money Behaviour Data"
        android:textColor="#99FFFFFF"
        android:textSize="12sp"
        android:alpha="0"
        android:id="@+id/tv_sub" />

    <!-- Loading spinner -->
    <ProgressBar
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginTop="48dp"
        android:indeterminateTint="@color/white" />

    <!-- SDG Badge -->
    <TextView
        android:id="@+id/tv_sdg_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="SDG 11 · SDG 6 · UNAI Principle 7"
        android:textColor="#CCFFFFFF"
        android:textSize="11sp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:background="@drawable/badge_bg"
        android:alpha="0" />

</LinearLayout>
